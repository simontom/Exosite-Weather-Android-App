apply plugin: 'com.android.application'
apply plugin: 'kotlin-android'
apply plugin: 'kotlin-android-extensions'
apply plugin: 'kotlin-kapt'


buildscript {
    repositories {
        jcenter()
    }
    dependencies {
    }
}

android {
    compileSdkVersion rootProject.compileSdkVersion
    buildToolsVersion rootProject.buildToolsVersion

    signingConfigs {
        debug {
            keyAlias 'androiddebugkey'
            keyPassword 'android'
            storeFile file('C:/Users/Simon/.android/debug.keystore')
            storePassword 'android'
        }
    }

    defaultConfig {
        applicationId "cz.saymon.android.exositeoneplatformrpctest"
        minSdkVersion 16
        targetSdkVersion 27
        versionCode 1
        versionName "1.0"

//        javaCompileOptions {
//            annotationProcessorOptions {
//                includeCompileClasspath true
//            }
//        }
    }

    buildTypes {
        debug {
            signingConfig signingConfigs.debug
        }
        release {
            signingConfig signingConfigs.debug
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }
}

repositories {
    jcenter()
    maven { url "https://clojars.org/repo/" }
    google()
}

kapt {
    generateStubs = true
}


dependencies {

    // Android Support libs (views) etc.
    compile fileTree(dir: 'libs', include: ['*.jar'])
    compile "com.android.support:support-v4:$rootProject.androidSupportVersion"
    compile "com.android.support:appcompat-v7:$rootProject.androidSupportVersion"
    compile "com.android.support.constraint:constraint-layout:$rootProject.constraintLayoutVersion"

//    compile "com.android.support:design:$androidSupportVersion"
//    compile "com.android.support:recyclerview-v7:$androidSupportVersion"
//    compile "com.android.support:cardview-v7:$androidSupportVersion"
//    compile "com.google.android.gms:play-services:$googleServicesVersion"

    // Other libs for Views

    //    compile 'org.quanqi:android-view-pager-indicator:0.9.0'

    //    compile 'com.makeramen:roundedimageview:2.2.1'

    // MVP architecture support

    //    compile "info.android15.nucleus:nucleus:$nucleusVersion"

    //    compile "info.android15.nucleus:nucleus-support-v4:$nucleusVersion"

    //    compile "info.android15.nucleus:nucleus-support-v7:$nucleusVersion"

    // Maps services and utils

    //    compile "com.google.android.gms:play-services-maps:$googleServicesVersion"

    //    compile "com.google.android.gms:play-services-location:$googleServicesVersion"

    //    compile 'com.google.maps.android:android-maps-utils:0.4.4'

    // EventBus

    //    compile 'org.greenrobot:eventbus:3.0.0'

    // Loggin; better than plain 'Log'
    compile 'com.jakewharton.timber:timber:4.4.0'

    // Persistent data helpers

    ///////////////////////////

    // SharedPrefs

    //    compile "de.devland.esperandro:esperandro-api:$esperandroVersion"

    //    kapt     "de.devland.esperandro:esperandro:$esperandroVersion"

    //    compile "de.devland.esperandro:esperandro-gson-addon:$esperandroVersion"

    // Database with RxJava support

    //    compile 'com.squareup.sqlbrite:sqlbrite:0.7.0'

    // OnSaveInstanceState helper

    //    compile "frankiesardo:icepick:$icepickVersion"

    //    apt     "frankiesardo:icepick-processor:$icepickVersion"

    // Dependency injection, Code generating

    /////////////////////////////////////////

    // Dagger
    implementation "com.google.dagger:dagger:$rootProject.daggerVersion"
    kapt "com.google.dagger:dagger-compiler:$rootProject.daggerVersion"

    //    provided 'org.glassfish:javax.annotation:10.0-b28'

    // ButterKnife

    //    compile "com.jakewharton:butterknife:$rootProject.butterKnifeVersion"

    //    annotationProcessor "com.jakewharton:butterknife-compiler:$rootProject.butterKnifeVersion"

    // AutoValue

    //    apt     "com.google.auto.value:auto-value:$autoValueVersion"

    //    compile "com.google.auto.value:auto-value:$autoValueVersion"

    // https://github.com/rharter/auto-value-gson

    //    apt     "com.ryanharter.auto.value:auto-value-gson:$autoValueGsonVersion"

    //    compile "com.ryanharter.auto.value:auto-value-gson:$autoValueGsonVersion"

    // https://github.com/rharter/auto-value-parcel

    //    apt     "com.ryanharter.auto.value:auto-value-parcel:$autoValueParcelVersion"

    //    compile "com.ryanharter.auto.value:auto-value-parcel-adapter:$autoValueParcelVersion"

    // https://github.com/square/auto-value-redacted

    //    apt     "com.squareup.auto.value:auto-value-redacted:$autoValueRedactedVersion"

    // https://github.com/gabrielittner/auto-value-cursor

    //    apt     "com.gabrielittner.auto.value:auto-value-cursor:$autoValueCursorVersion"

    //    compile "com.gabrielittner.auto.value:auto-value-cursor-annotations:$autoValueCursorVersion"

    // Networking

    //////////////

    // Image Loading

    //    compile 'com.github.bumptech.glide:glide:3.7.0'

    //    compile 'com.nostra13.universalimageloader:universal-image-loader:1.9.5'

    // Network

    //    compile 'com.loopj.android:android-async-http:1.4.9'

    //    compile "com.squareup.okhttp3:okhttp:$okhttp3Version"
    compile "com.squareup.okhttp3:logging-interceptor:$rootProject.okhttp3Version"

    // Retrofit and its helper parts
    compile "com.squareup.retrofit2:retrofit:$rootProject.retrofitVersion"
    compile "com.squareup.retrofit2:converter-gson:$rootProject.retrofitVersion"
    compile "com.squareup.retrofit2:converter-scalars:$rootProject.retrofitVersion"
    compile 'com.google.code.gson:gson:2.8.1'

    // Reactive
    compile "io.reactivex.rxjava2:rxjava:$rootProject.reactivexVersion"
    compile "io.reactivex.rxjava2:rxandroid:$rootProject.reactivexAndoirVersion"
    compile "com.squareup.retrofit2:adapter-rxjava2:$rootProject.retrofitVersion"
    compile "com.jakewharton.rxbinding2:rxbinding:$rootProject.rxBindingVersion"
    compile "org.jetbrains.kotlin:kotlin-stdlib-jre7:$rootProject.kotlin_version"

    //    compile "com.jakewharton.rxbinding:rxbinding-support-v4:$rxBindingVersion"

    //    compile "com.jakewharton.rxbinding:rxbinding-appcompat-v7:$rxBindingVersion"

    //    compile "com.jakewharton.rxbinding:rxbinding-design:$rxBindingVersion"

    //    compile "com.jakewharton.rxbinding:rxbinding-recyclerview-v7:$rxBindingVersion"
}
